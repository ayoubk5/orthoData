// Central API configuration — change VITE_API_URL in the root .env file
// to switch between localhost and server deployment.
export const API_URL = import.meta.env.VITE_API_URL || 'http://10.4.28.11:5000';
